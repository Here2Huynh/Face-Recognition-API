POST requests are never cached 
POST requests will not remain in the browser history 
No restriction on data length
Uses HTTPS = HTTP + SSL/TLS 

Never store password in plain Text. 
Hash the password. 

